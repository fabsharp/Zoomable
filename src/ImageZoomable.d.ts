export default class ImageZoomable {
    parent: HTMLElement;
    private img;
    private zoomable;
    private initialScale;
    constructor(parent: HTMLElement, src: string);
    private bound;
    private responsive;
}
